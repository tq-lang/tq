#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
SCRIPT="$REPO_ROOT/scripts/generate-changelog.sh"

if [[ ! -x "$SCRIPT" ]]; then
  echo "tq: missing executable changelog script: $SCRIPT" >&2
  exit 1
fi

bash "$SCRIPT"
git add "$REPO_ROOT/CHANGELOG.md"
